{
  "skill_name": "auto_context",
  "evals": [
    {
      "id": 0,
      "name": "white_rabbit_alice",
      "prompt": "Our team just spun up a Postgres+pgvector instance for a RAG service we want to expose internally — the server is running locally on localhost:5436 against database `ragtest` with user `ragtest_user` / password `ragtest_pass`. The schema is already in place: a `documents` table with columns `(id BIGINT PK, source TEXT, chunk_idx INT, content TEXT, embedding vector(384))`, plus an HNSW index on embedding (cosine) and a GIN FTS index on content. The corpus I want to index lives at /Users/weixin/workspace/skardi-skills/rag_test_corpus/ — it's a few markdown files with public-domain literary excerpts. The Skardi binaries are at /Users/weixin/.local/bin/skardi-server (release, built with --features candle) and /usr/local/bin/skardi (CLI). Please ingest that corpus through skardi-server and then answer this question, grounded in retrieved chunks with citations: **Who is the White Rabbit, and what specifically about him makes Alice notice him?** Use port 8085 for the server. Save the final answer as a markdown file at outputs/answer.md.",
      "expected_output": "A grounded markdown answer at outputs/answer.md citing alice_sample.md, mentioning the pink eyes, the waistcoat-pocket watch, and the catchphrase 'Oh dear! Oh dear! I shall be late!'",
      "files": []
    },
    {
      "id": 1,
      "name": "bessie_jane_eyre",
      "prompt": "I have a Postgres+pgvector database running on localhost:5437 (db `ragtest`, user `ragtest_user`, password `ragtest_pass`). The `documents` table has already been created with `(id BIGINT PK, source TEXT, chunk_idx INT, content TEXT, embedding vector(384))`, an HNSW cosine index on embedding, and a GIN FTS index on content — same shape across all our RAG tables. The corpus is at /Users/weixin/workspace/skardi-skills/rag_test_corpus/ (literary excerpts in markdown). Skardi binaries at /Users/weixin/.local/bin/skardi-server (release, built with --features candle) and /usr/local/bin/skardi (CLI). Please ingest the corpus via skardi-server and answer this with retrieval-grounded citations: **What role does Bessie play in Jane Eyre, and quote a passage that mentions her or shows her interacting with Jane.** Use port 8086 for the server. Save the answer as outputs/answer.md.",
      "expected_output": "A grounded answer at outputs/answer.md identifying Bessie as the nurse, with a verbatim quote from jane_eyre_sample.md (likely chapter I or II) that names her, and citation to chunk indices.",
      "files": []
    },
    {
      "id": 2,
      "name": "art_of_war_translation_history",
      "prompt": "We want to set up a hybrid-search service over our public-domain corpus, served from skardi-server on top of a Postgres+pgvector DB we've already provisioned. The DB is at localhost:5438, database `ragtest`, user `ragtest_user`, password `ragtest_pass`. The `documents` table already exists with shape `(id BIGINT PK, source TEXT, chunk_idx INT, content TEXT, embedding vector(384))` and the right indices (HNSW cosine on embedding, GIN FTS on content). The corpus lives at /Users/weixin/workspace/skardi-skills/rag_test_corpus/. Skardi binaries: /Users/weixin/.local/bin/skardi-server (release, --features candle) and /usr/local/bin/skardi. Ingest the corpus via skardi-server (port 8087) and answer this from retrieved chunks with citations: **How was The Art of War first introduced to Europe, and which English translation does the preface call 'excessively bad'?** Save as outputs/answer.md.",
      "expected_output": "A grounded answer at outputs/answer.md naming Joseph Amiot's 1782 French translation as the first European introduction and Capt. E. F. Calthrop's 1905 English translation as the one labelled 'excessively bad', with citations to art_of_war_sample.md chunks.",
      "files": []
    },
    {
      "id": 3,
      "name": "local_folder_zero_config",
      "prompt": "I have a folder of engineering notes at ./notes (markdown, about 40 files) and I want you to make it searchable so you can answer questions from it and cite where each answer came from. I don't have any database set up and I'd rather not stand one up. Just make it work locally.",
      "expected_output": "Takes the local path without asking the user to choose a storage backend: runs setup_context.py with the default --backend sqlite (no --connection-string, no --table), so the skill creates and owns <workspace>/kb.db. Before that it checks the interpreter can load SQLite extensions (macOS /usr/bin/python3 cannot) and picks a capable one. Confirms the embedding choice with the user rather than silently picking one. Exports the SQLITE_VEC_PATH line that setup printed before starting the server, starts it with --runtime local-process (NOT docker — sqlite under docker is refused), ingests ./notes, then answers from retrieved chunks citing `source`. Does not claim a serverless or CLI-only mode exists.",
      "files": []
    },
    {
      "id": 4,
      "name": "local_reingest_no_data_loss",
      "prompt": "I set up that local knowledge base over ./notes yesterday and already ingested everything. I just added three new files to the folder — please get them in too.",
      "expected_output": "Does NOT re-run setup_context.py with --force: the workspace and kb.db already exist and --force would drop every ingested row. Reuses the existing workspace, re-exports SQLITE_VEC_PATH, starts the server, and re-runs ingest_corpus.py — which journals progress, so the already-ingested files are skipped and only the three new ones are sent. Reports how many were skipped versus ingested.",
      "files": []
    },
    {
      "id": 5,
      "name": "existing_table_read_only",
      "prompt": "We keep our support macros in a SQLite file at ./ops/macros.db — table `macros`, columns (slug TEXT PRIMARY KEY, url TEXT, body TEXT), about 300 rows. Make those searchable so you can answer support questions from them with citations. Whatever you do, don't modify that file — other tooling owns it.",
      "expected_output": "Routes to the existing-table raw-material entry, NOT the folder entry (no exporting rows out to files first) and NOT the override storage path (macros.db is raw material, not where the index lives). Renders a normal local workspace, starts the server, then runs ingest_table.py --db ./ops/macros.db --table macros --key-column slug --content-column body --source-column url. Never writes to macros.db (the script opens it read-only) and never creates schema inside it. Cites the url values as sources when answering.",
      "files": []
    },
    {
      "id": 6,
      "name": "fetch_and_land_reconciliation",
      "prompt": "Our internal wiki (HTTP API, cursor-paginated listing endpoint, one-page-per-call content endpoint, token in WIKI_TOKEN) has around 200 pages. Index it so you can answer questions from it with citations.",
      "expected_output": "Follows references/fetch_and_land.md instead of inventing an ad-hoc crawl: first lands the COMPLETE listing (paging until the API's own end signal) into a staging SQLite table with key/source/title and empty content, then fetches bodies one page at a time into that table with per-row error recording, then RECONCILES — reports listed vs fetched counts and names the missing pages to the user before ingesting, and does not proceed past a mismatch without the user's explicit go-ahead. Only then ingests via ingest_table.py and answers with citations. Does not claim the index is complete without showing the reconciliation numbers.",
      "files": []
    }
  ]
}