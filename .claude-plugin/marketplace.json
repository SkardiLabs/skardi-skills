{
  "name": "skardi-skills",
  "owner": {
    "name": "Skardi Labs",
    "url": "https://github.com/SkardiLabs"
  },
  "metadata": {
    "description": "Agent Skills for working with Skardi — SQL pipelines as HTTP endpoints. Distributes plugins that teach AI coding agents Skardi's patterns for building searchable context, hybrid search, and connectors.",
    "version": "0.4.0"
  },
  "plugins": [
    {
      "name": "auto-context",
      "source": "./auto-context",
      "description": "Turn a folder of documents, a table you already have, or documents still inside a service (a wiki, cloud docs, a mailbox) into governed, searchable context an agent can query. Hybrid search (vector + full-text) served over HTTP by skardi-server. Three raw-material entries, one flow: a folder; an existing table (read-only; NDJSON for any datastore); or fetch-and-land, where your agent writes the per-source fetch code and the skill fixes the process and its reconciliation. Storage is a separate choice: defaults to a local SQLite file the skill creates and owns, or point it at Postgres+pgvector, MongoDB, or Lance when the index should live in a database you already own.",
      "category": "rag",
      "keywords": [
        "skardi",
        "context",
        "rag",
        "knowledge-base",
        "hybrid-search",
        "sqlite",
        "pgvector",
        "mongodb",
        "lance",
        "skardi-server"
      ]
    },
    {
      "name": "retrieval",
      "source": "./retrieval",
      "description": "Answer questions from live data through a running skardi-server using the skardi CLI. Discovers sources, pipelines, and the table schemas the deployment exposes, searches any semantic surface first, then writes read-only SQL against exact qualified table names, checks truncation before trusting counts, and reports with the query attached.",
      "category": "query",
      "keywords": [
        "skardi",
        "retrieval",
        "query",
        "sql",
        "hybrid-search",
        "skardi-cli",
        "data-agent",
        "read-only"
      ]
    },
    {
      "name": "graph-source",
      "source": "./graph-source",
      "description": "Connect a property graph (knowledge graph, GraphRAG corpus, Apache AGE / openCypher data) to Skardi and query it through SQL, end to end: provision the AGE backend with a least-privilege role, declare the type: graph source and its views in context YAML, verify registration health (healthy/degraded/refused), write correct queries (cypher_query, graph_schema, the JSON getters), and wire Cypher parameters into pipelines. Encodes the traps: positional column binding, no predicate pushdown into view Cypher, wrong-getter silent NULLs, and the one working {params} pipeline spelling.",
      "category": "query",
      "keywords": [
        "skardi",
        "graph",
        "cypher",
        "age",
        "opencypher",
        "knowledge-graph",
        "graphrag",
        "postgres",
        "read-only"
      ]
    },
    {
      "name": "graph-rag",
      "source": "./graph-rag",
      "description": "Answer natural-language questions whose answer lives in relationships, over a skardi-server that has both a graph source and a retrieval surface: seed semantically (or by property lookup), expand across the graph with cypher_query, synthesize with both hops cited. Complements graph-source (which connects the graph) and retrieval (which answers row questions) — reach for this when the answer needs edges, not rows.",
      "category": "query",
      "keywords": [
        "skardi",
        "graph-rag",
        "graphrag",
        "knowledge-graph",
        "cypher",
        "age",
        "multi-hop",
        "retrieval",
        "read-only"
      ]
    }
  ]
}
